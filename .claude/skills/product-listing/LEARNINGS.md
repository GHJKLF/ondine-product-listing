# product-listing skill learnings

Append-only. One entry per friction found during a run: date, product, what the skill got wrong/missed/left unclear, the fix. The manager merges entries into SKILL.md, profiles, references, scripts or the Higgsfield pack, then marks them `merged vX.Y.Z`.

## Open

- 2026-09-10 · Lanna Floral Button Maxi Dress · Exact-title/source-URL duplicate searches missed an unmanaged Ondine DRAFT (10600830664970, Brown Floral Print Maxi Dress with High Waist & Pockets); broader construction-word search and direct image comparison found its photo is exactly the source Khaki colourway → stop new-product creation and prepare an update to that exact existing record for Ilias; title-only zero matches are insufficient duplicate clearance.

- 2026-09-10 · Lanna Floral Button Maxi Dress · Earlier run treated absent composition/care as a ban on preparing any draft, despite source rules explicitly permitting missing facts as flags; the all-five acceptance rule conflicts with unknown category attributes staying blank → after Ilias accepted preparation using confirmed facts, save an incomplete review DRAFT with unknowns recorded internally and customer-facing fabric/care omitted; do not claim the full data-ready or activation gate has passed.

- 2026-09-10 · Lanna Floral Button Maxi Dress · Authenticated Firecrawl and the direct source read agree on price, options, variants, gallery and measured lengths, but the source supplies neither composition nor care instructions; Ondine requires verified fabric/care data and all five required metafields → obtain supplier composition and care evidence before creating the DRAFT; do not substitute a guessed fibre or care line.

- 2026-09-10 · Lanna Floral Button Maxi Dress · Initial read-only Shopify duplicate query omitted a final GraphQL closing brace and returned a syntax error → validate and re-run the corrected query before any product mutation; no write was attempted.

- 2026-09-10 · Lanna Floral Button Maxi Dress · Shopify preflight found the connected store was Nelmoo (`dc7cqq-c2.myshopify.com`, EUR), not the required Ondine London store (`zfrbm1-y6.myshopify.com`, GBP) → switch the existing Shopify connector to Ondine before any duplicate check or DRAFT write; no Shopify data was changed.

- 2026-09-10 · Lanna Floral Button Maxi Dress · Ilias explicitly authorized the no-Firecrawl fallback; direct live-page review plus `<product>.js`, `/cart.js`, and the product-bound size modal agreed on GBP price, six colours, 30 variants and size mapping, but did not establish composition, care or model facts → retain it as a read-only proposal only; do not create a Shopify DRAFT until the authenticated Firecrawl requirement is restored.

- 2026-09-10 · Lanna Floral Button Maxi Dress · The Firecrawl CLI became available through `npx firecrawl-cli@latest`, but reported `Not authenticated` → run `npx -y firecrawl-cli@latest login --browser` (or configure an authorized API key) before the required authenticated source extraction; no Shopify preflight or write occurred.

- 2026-09-10 · Lanna Floral Button Maxi Dress · The product-listing workflow requires the authenticated Firecrawl CLI as the primary source extractor, but `firecrawl` is unavailable in this session → restore the authenticated CLI before resuming; do not replace it with browser-only extraction for a Shopify DRAFT.

- 2026-09-10 · Ondine master sheet · Live next-product selection could not be checked because the configured `gws` OAuth token returned `invalid_grant` → re-authenticate the workspace with `gws auth logout` then `gws auth login`; do not infer the next row from historical notes.

- 2026-09-10 · Verity wine ikat maxi · A parallel five-shot image generation request was stopped by output moderation before any selected artifact could be returned → retry each shot independently with neutral garment framing and preserve the approved lead as the continuity reference.
- 2026-09-10 · Verity wine ikat maxi · Current Shopify productCreate rejects explicit option values when linkedMetafield already supplies size values, despite the step-4 mechanic saying to send both → retry with only linkedMetafield.values, then read back the generated option-value IDs before variant creation.

- 2026-09-09 · Ondine default channels · Interpreted “keep draft until Haider review” as no sales-channel assignments, requiring manual selection after activation → Ilias corrected the policy; canonical skill/profile/checklist now select Online Store and Google & YouTube while preserving DRAFT.

- 2026-09-08 · Munez · Both ghost outputs filled a verified low V-back with an invented high pale inner panel → reject as a construction error; targeted image edit removed only that panel, and independent review confirmed the corrected open neckline before gallery approval.

- 2026-09-08 · Munez · Worn-waist detail output was blocked by image-service moderation; movement framing returned .856/.836 against .80 maximum → prepared textile-only waist detail as a safer fallback; retain uncropped second movement attempt under the documented one-retry rule.

- 2026-09-08 · Munez · Remaining-view prompts inherited initial-hero “distinct from all reference models” identity wording and ghost reference metadata preserved model identity despite model-free rules → clarified run prompts 02–05 to preserve generated hero while excluding competitor likeness, and slot 06 to use hero for garment/studio only; canonical renderer should distinguish initial casting from continuity references.
- 2026-09-08 · Munez · 01b inherits source-only reference role despite taking the generated hero; square continuity still declares a new baseline → reviewer corrected run mappings to preserve hero garment/studio only for 01b and preserve existing full identity for square; update canonical reference metadata later.
- 2026-09-08 · Munez · Styling variety checker counts negated accessory names, so pearl drops plus “no necklace/bracelet” falsely matched a prior look → state positive accessories only; preserve exclusions in the model/template rules.
- 2026-09-08 · Rebecca · Movement framing 85.2% then 88.1% and ghost framing 84.2% then 80.7% exceed 80% template cap; ghost also invented a label and skin-like sleeve fill → preserve best uncropped selection with explicit exceptions; use image edit to remove label/fill; improve wide-framing templates rather than endless retries.
- 2026-09-08 · Rebecca · Square generation returned HTTP500 without an artifact; identical retry succeeded → preserve successful portrait, check native outputs before retry, record provider failure separately from framing attempts.
- 2026-09-08 · Rebecca · Lead portrait occupancy 92.1% then 91.9% after identical retry exceeds 90% target → retained complete uncropped second attempt under approved marginal-overshoot rule; framing template still needs wider composition.
- 2026-09-08 · Rebecca · Styling brief logging assumes every extra decision value is a string; second-model object causes a print error after successful writes → keep structured model selection outside the flat decisions map until logger supports objects.
- 2026-09-08 · Rebecca · Normal FactPacket registry is hard-pinned to the Calloway fixture; independent product review has no supported registration path, blocking new listing validation → add a production registration workflow with distinct author/reviewer identities; never claim a fixture or test-mode pass for a live product.

- 2026-09-08 · Gini metallic maxi · productSet rejects values alongside linkedMetafield on an option, unlike productCreate → provide only linkedMetafield.values for the size option; read before retrying.

- 2026-09-08 · Gini metallic maxi · Correction to earlier blocker: stopped without opening Find your size interactive widget; user revealed product-bound body chart, including UK 8 hips 89 cm versus generic page 91 cm → inspect interactive size-guide tabs before declaring measurements missing; use product-bound chart and preserve conflicting generic evidence.

- 2026-09-08 · Gini metallic maxi · User requires product-specific letter and UK sizing, but source has numeric-only sizes and a generic reference chart; no supplier record found → obtain supplier chart and verified equivalence before DRAFT creation; do not infer letters from the general Ondine conversion.
- 2026-09-07 · Mayfair pleated midi · Ilias approved UK equivalents plus source letters together (UK 6–8 (S), UK 10–12 (M), UK 14 (L), UK 16 (XL), UK 18–20 (2XL)); numeric-only/letter-only rules must not override this decision. Haider only does product research; remove stale verification/activation assignment. Built-in ChatGPT generated previews. → Preserve one real source size, its source chart, and approved combined labels.
- 2026-09-07 · Mayfair pleated midi · Ilias approved product-specific charts with the general chart retained as fallback → updated profile precedence, retired automatic paired-size expansion, prepared chart CSV/HTML for internal review; exact product assignment awaits a draft and confirmed sizing.
- 2026-09-07 · Mayfair pleated midi · comparing source chart to live Ondine Kiwi chart exposed unknown body-versus-garment measurement basis and UK labels inconsistent with Ondine measurements → do not infer fit from nearest numbers; obtain supplier measurement basis and ease/stretch before assigning single UK sizes. Ilias approved no automatic expansion of one supplier size into paired UK variants.
- 2026-09-07 · Mayfair pleated midi · live product-specific UK size chart conflicts with the fixed Ondine letter conversion (S=6–8 versus 8–10; differences throughout) → resolve source-chart precedence before writing variants; never silently apply the generic conversion.
- 2026-09-07 · Mayfair pleated midi · Ilias removed the covered/long-sleeve/occasionwear selection restriction → profile scope changed to UK women's fashion and synced; garment-specific attributes remain fact-based.
- 2026-09-07 · listing workflow · Ilias specifies built-in ChatGPT image generation and sheet status Ready for Haider review with admin link; Haider owns final verification and activation → apply these session overrides, retaining the three visual gates.

## Merged

- 2026-09-13 · Full skill review · Reconciled seven-image checklist, prose-only copy, source-size preservation, unknown-field handling, inventory tracking exception, second-model colour adaptation, retained-media cleanup and provider metadata. Added operator readiness/dependency/update guidance. Historical entries and signed fixtures remain unchanged. Compiler remains blocked by pre-existing signed-artifact mismatch; 57 tests ran with 33 failure reports, chiefly PHASE_2_LOCK_INVALID. This is a documentation maintenance revision, not a certified runnable release.
- 2026-09-03 · manager · a worker logged "the storefront has NO size guide"; wrong, the guide is the Kiwi Size Chart app block in product.json, its data lives in the app → profile names Kiwi explicitly; workers must not infer a store gap from a missing page/snippet — merged v4.6.0
- 2026-09-03 · manager · Google & YouTube channel flagged "Missing value [color]" on every Size-only product; the channel reads colour from a Colour variant option, not from shopify.color-pattern → profile: always create a Colour option, even single-value — merged v4.6.0
- 2026-09-03 · Hobbs Thea Dress · the "size chart stops at UK 18" FLAG has now recurred on three runs (Wright Mayra 20-28, Ever-Pretty, Thea 20-22) because flagging is all a run can do; Ilias had to ask "did we not fix this last time" → chart extended to UK 28 on the existing grade (+7cm per size above 18) in the vault CSVs, and separately the storefront has NO size guide at all (no /pages/size-guide, no theme snippet, nothing on a live PDP), so the profile line calling the CSVs "the published size chart" is wrong; the skill should either stop re-flagging a known store gap or point at an owner — merged v4.6.0
- 2026-09-03 · Hobbs Thea Dress · Higgsfield pack v4.4.0 fed one `{{product.pose}}` into slots 01/gmc/02/03/05, so those slides read as the same picture; Ilias rejected slot 05 twice then 01-vs-03 and asked for the fix to live in the template → per-slot `pose_01/02/03/05` placeholders, `decisions.poses` in the styling brief, and a near-duplicate refusal in apply_styling_brief.py (shipped as pack v4.5.0 in this run before rule 7 arrived — manager to confirm keep or roll back; detail in scratchpad/thea/LEARNINGS.md) — merged v4.6.0
- 2026-09-03 · Hobbs Thea Dress · slot 05 `view_yaw_degrees` allowed only 0-40, which contradicts any lifestyle pose that turns away from the lens → widened to 0-160 and renamed the view; a lifestyle frame may legitimately face into the room — merged v4.6.0
- 2026-09-03 · Hobbs Thea Dress · `productVariantsBulkCreate` rejects both `optionValues.name` ("Cannot set name for an option value linked to a metafield") and `optionValues.linkedMetafieldValue` ("id or name must be specified") when options are linked to shopify.size / color-pattern / size-type; cost two failed writes → skill should state that linked-option variants must use the `ProductOptionValue` **id** read back after productCreate — merged v4.6.0
- 2026-09-03 · Hobbs Thea Dress · source sells Petite and Regular as two separate product pages, not one option; profile covers Size and Colour only → document mapping a fit dimension onto `shopify.size-type` (had to create the `Petite` metaobject), and add a colour-code rule for print-qualified colours like "Blue Floral" (used BLU) — merged v4.6.0
- 2026-09-03 · Hobbs Thea Dress · the two fits state conflicting composition (Regular 100% polyester fully lined 144.5cm, Petite 100% viscose unlined 136cm) and the skill has no rule for a per-fit split → say explicitly whether to list both per fit, split into two products, or stop; Ilias chose list both — merged v4.6.0
- 2026-09-03 · Hobbs Thea Dress · source is Salesforce Commerce Cloud, so Step 1's `<url>.json` and `/cart.js` both 404 → name the SFCC fallback (ld+json for price/currency, `size-capsules__list_item` markup for per-size stock) — merged v4.6.0
- 2026-09-03 · Hobbs Thea Dress · slot 03 missed the 0.80 height cap on three renders (0.829, 0.824, 0.809) and slot 06 missed the 10% bottom margin twice (7.3%, 9.0%), same pattern as the Wright Mayra run → the caps are too tight for a full-length maxi at 3:4; suggest slot 03 max 0.84 and slot 06 bottom 0.08 — merged v4.6.0
- 2026-09-03 · Hobbs Thea Dress · `TaxonomyAttribute` has no `name` field, so the obvious category-attribute query errors → pin the working form `... on TaxonomyChoiceListAttribute { id name values { nodes { id name } } }` in the skill — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Skill says "use the existing Shopify connector" but never says how to find it; this session started with no Shopify MCP and the server was named by opaque UUID, not `claude_ai_Shopify` → SKILL.md step 3 should say to locate the tools via ToolSearch for `get-shop-info`/`graphql_mutation` whatever the server is called, and that the user may need to attach it first. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · `productCreate` with a linked option fails ("metafield has no values") unless the same call also sets the matching product metafield (`shopify.color-pattern`, `shopify.size`) listing exactly those option values → document in SKILL.md step 4. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · `productVariantsBulkCreate` rejects option value *names* on linked options ("Cannot set name for an option value linked to a metafield"); it needs `{optionId, id}` GIDs → always request `options { id optionValues { id name } }` back from productCreate and document the ID-based variant payload. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · The `Green` colour metaobject did not exist and there is no `shopify--pattern` definition on this store → document the create-if-missing `metaobjectCreate` step (label, color hex, color_taxonomy_reference, pattern_taxonomy_reference) and that pattern cannot be set separately. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Shopify neckline taxonomy has no "High neck"; closest is `Mock` → list the accepted neckline values in the Ondine profile so workers do not stall on a missing value. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Profile colour map had no `Burgundy` and reads like a STOP condition; Ilias had to say "never ask, just add it" → reword the profile to "add the missing code yourself and move on", no ask and no FLAG. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Firecrawl with `--country GB` still returned USD 114 and only the Green colourway in JSON-LD; the browser was geolocated to MA. Switching market via the store's own `/localization` form revealed GBP 90 and all three colourways (12 variants) → step 1 must require reading the storefront market state (`window.Shopline.currency`, cookies) and switching market through the site's own form before trusting price or option set. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Master sheet said £86/15 reviews; live UK page said £90.00/24 reviews → state explicitly that the sheet is a pointer, never a price source. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · The lead colourway had no back photo, but the other two colourways did; unclear whether that satisfies GARMENT_BACK_EVIDENCE → say in the reference-role taxonomy that a back photo of another colourway is valid (the role establishes construction only, never colour), so a worker does not stop. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Higgsfield rejects the rendered prompt object ("prompt should be string, got object"); it must be passed JSON-string-encoded → have `render_slot_prompt.py` also write the `.arg` file and show the exact CLI line in gallery-workflow.md. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Slots 02/03/05 reference `{{product.pose_02|03|05}}` but `apply_styling_brief.py` writes only `pose`, so `render_slot_prompt.py` raises KeyError → §7.0 question 6 should ask for four poses (front, back, movement, lifestyle) and the script should write all four. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Biggest quality gap: default facts rendered plain jersey as smooth airbrushed bands; Ilias rejected two passes as "brush paint not human". Describing the material is not enough — describing the *capture* fixed it (real studio photograph, 85mm f/8 softbox, visible knit grain, fibre highlights on ruching ridges, irregular pull-lines, retained sensor noise, skin pores and flyaways, plus an explicit ban on digital painting / airbrushed gradient / CGI cloth / over-smoothing) → add this as a fixed brand-constant block in the pack so every slot gets it; solid-colour garments need it far more than prints. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Higgsfield multi-image-reference calls failed for ~15 min ("request failed (no response received)") while single-reference calls succeeded, then recovered → gallery-workflow.md should prescribe a retry loop and note a failed create leaves no job, so retrying cannot duplicate. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · `SendUserFile` cannot deliver the 4K masters to Remote Control (8.9 MB, "Socket is closed") → tell workers to send a ~1600px preview for approval gates and keep the master on disk for upload. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Pack v4.5.0 migration mid-run cost nothing: putting the four hand-written poses into `decisions.poses` and re-running the script changed only `pose_01`, so no approved or in-flight image had to be regenerated → worth stating in gallery-workflow.md that re-running the brief script is idempotent, so a mid-run pack bump is safe to adopt without discarding work. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Slot 03 template contradicts itself on centring: `layout.subject_center_tolerance.x` is 0.1 but acceptance condition `S03_CENTER_X` allows only 0.05; a frame at cx 0.564 passes one and fails the other → make the two agree (the movement slide legitimately sits off-centre because the bbox includes the fabric arc, so 0.1 looks like the intended value). — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · The Higgsfield CLI cannot send the slot JSON verbatim: passing the object errors ("prompt should be string, got object") and passing it JSON-string-encoded stores it as a quoted, backslash-escaped string, so the payload is not the object the pack's determinism contract requires (confirmed in the job record). The MCP generate_image / generate_image_batch path takes the prompt as a clean string and works with upload UUIDs from `higgsfield upload create` → make the MCP path the documented route in gallery-workflow.md and keep the CLI only for uploads. Slot 01, the GMC square, slot 04 and slot 06 in this run were produced via the CLI before this was known; 02/03/05 and both colourway fronts were re-rendered through MCP. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · Solid-colour draped garments invite an invented free-hanging sash: slots 02, 03 and 05 all grew a scarf-like panel falling clear of the body from the shoulder, because the facts said the drape "falls loose along the outside of the arm" without saying it is seamed down. Fixed by stating the drape is joined into the sleeve and side seam, that the back is completely plain, and adding sash/cape/streamer/train to the not-allowed list → gallery-workflow.md §7.1 should require an explicit attached-or-free statement for every drape, tie, panel or sleeve detail, and the pack's prohibited list should carry the free-hanging-fabric terms by default. — merged v4.6.0
- 2026-09-03 · swan-song draped maxi · `measure_framing.py` is meaningless for slot 04 (close-up) and slot 05 (lifestyle interior): both returned height ~0.99 with zero margins because there is no plain backdrop to measure against → the script should skip or flag these two slots rather than report a false framing miss. — merged v4.6.0
- 2026-09-03 · Ever-Pretty ES0106B · Higgsfield CLI `generate create` rejects the rendered prompt object, and the JSON-string-encoded workaround makes the CLI store the ORIGINAL quoted text, so the API receives the slot JSON wrapped in literal double quotes (not verbatim); outputs still passed but the payload is not reproducible → gallery-workflow.md should route generation through the Higgsfield MCP `generate_image` (params.prompt as a plain string) rather than the CLI
- 2026-09-03 · Ever-Pretty ES0106B · `higgsfield generate wait` has no `--wait-timeout` flag on this build and its result link is the compressed `_min.webp` derivative, not the 4K master → name MCP `jobs_wait` as the way to collect results; its `result_url` is the full-resolution `.png`
- 2026-09-03 · Ever-Pretty ES0106B · colour wording guessed from the retailer swatch name failed garment fidelity on 3 of 7 colourways (Damson rendered dark plum vs a dusty mauve source, Dark Green forest vs emerald, Burgundy purple-leaning vs red wine); only caught by building a source-vs-render comparison → §7.3b should require a side-by-side colour check against the source photo of every extra colourway before the gallery gate, and say colour wording is written from the photograph, never the label
- 2026-09-03 · Ever-Pretty ES0106B · slot 01 retry rule gives no tiebreak when both attempts miss: attempt 1 was height 0.903 with a complete hem, the mandated identical resubmit came back 0.917 with the hem ON the bottom edge (a real crop failure) → say that when both miss, keep the one satisfying the uncropped-garment rule and treat a marginal occupancy overshoot as acceptable
- 2026-09-03 · Ever-Pretty ES0106B · `measure_framing.py` reported `left 0.012 / cx 0.357` on slot 02 because the backdrop's vertical falloff crosses its 110 threshold at the frame edge; the subject was in fact centred → note that a left/right reading near 0 with a sane height means the gradient tripped the heuristic, judge those slots visually
- 2026-09-03 · Ever-Pretty ES0106B · the brief asks Ilias "how many extra colourway fronts to render" at the gallery stage, but he answered by narrowing which colourways the PRODUCT carries (20 → 8, 248 → 98 variants), which had to be settled before productCreate → split it into two questions: which colourways the listing carries (before the DRAFT write) and how many fronts to render (at the gallery stage)
- 2026-09-03 · Ever-Pretty ES0106B · "only popular colours in UK in autumn" had no documented basis in the Ondine profile, so the 8-of-20 pick was my judgement alone → if seasonal curation recurs, add a short seasonal palette rule to the profile so two runs make the same call
- 2026-09-03 · Ever-Pretty ES0106B · source colourway images are split across `/files/` and `/products/` CDN paths and guessing the wrong one 404s → say to take the exact `featured_image.src` from `<product>.js` rather than reconstructing a path
- 2026-09-03 · Ever-Pretty ES0106B · pack moved v4.4.0 → v4.5.0 mid-run after slots 02/03/05 were already generated against the shared pose; cost 3 regenerations (slot 01 kept, approved) → the per-slot pose change was right and the old gallery did read repetitive, no fix needed beyond noting that a mid-batch pack bump means re-rendering prompts and regenerating only the pose-bearing slots
- 2026-09-07 · Pipeline handoff, latest Ilias instruction supersedes earlier Haider ownership/status notes · After listing, set sheet Status exactly Draft. Haider reviews the PDP, changes Shopify status to Active, then updates sheet Status to Active. Legend, dropdowns, colours and dashboard aligned; no automatic activation. Do not use Listed — Draft complete or Ready for Haider review.
- 2026-09-07 · Full colour galleries · User requested all six shots per colour, beyond the current extra-front-only rule → generate missing colour × shot combinations from the approved colour front plus approved matching Green shot; preserve existing fronts and record exact Colour/shot_order/path/approval status in a manifest. 15 new images plus two ghost framing retries; minor residual ghost framing variation remains for visual review. No upload approval inferred from generation request.

- 2026-09-07 · Ilias authorised skill update · Incorporated full lead-colour approval → colour-only six-shot adaptations, reuse of approved assets, built-in ChatGPT transport, colour/shot manifest and explicit visual/upload gates into SKILL.md, gallery-workflow.md and Ondine profile. Framing heuristic remains unchanged; residual deviations must be reviewed.

2026-09-08 — Ilias approved two distinct models in the first two gallery positions. New galleries have seven PDP images; preserve back, movement, detail, lifestyle and garment-only coverage. Keep original template IDs and add 01b; schema-version 2 distinguishes seven-view manifests from legacy six-view records.

## Gallery maintenance — merged 2026-09-09

- 2026-09-09 · Twelve ACTIVE Ondine galleries · Draft-only compiler was unsuitable for media updates → documented distinct product-image-set route using existing serializer, fresh IDs/digest, nonmedia preservation and retained older media; listing DRAFT gate unchanged.
- 2026-09-09 · Five held galleries · Internal QA rejection and explicit user approval were conflated → separate historical QA, scoped approval and upload authorization; approved candidates are not re-rejected for the same disclosed concern. Missing views still require generation.
- 2026-09-09 · Gallery coordination · Checkpoints stayed awaiting-slot during uploads, and older skill/profile contradicted current provider and seven-view order → document phase-entry updates, serialized upload ownership and authorized archiving; replace obsolete Higgsfield/six-view/delete-old-media instructions. Mirror sync and reference validation required.
- 2026-09-09 · Ruched drape · Defaulting unspecified panels to attached concealed a real cuff-attached hanging panel → preserve observed attachment points and mark unknown construction unknown, never invent a universal no-sash rule.

### 2026-09-13 — Checker maintenance repair

Ilias authorized repair of the outdated checker. Added a separately pinned maintenance record for current profile/composition documents; original historical signed records are unchanged. Production media validation now requires seven ordered slots including 01b and the current review gate. Historical fixture compatibility remains noncommittable, with regression coverage. All 63 offline tests pass (57 existing, six new). This is not a live Shopify test or Haider readiness certification; product-specific registry support, client setup and end-to-end operator verification remain pending.
